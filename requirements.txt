# ── Core ──────────────────────────────────────────────────────────────────────
flask>=3.0.0
openpyxl>=3.1.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── AI Providers (install the one matching your AI_PROVIDER env var) ──────────
# openai provider:
openai>=1.0.0

# anthropic provider:
anthropic>=0.25.0

# ollama: runs locally, no package needed — just install Ollama from ollama.ai