# Android App Track (Planned)

This folder is reserved for the future minimal APK forwarder.

Target scope:

- Headless background worker
- SMS capture
- Durable queue + retries
- Signed webhook delivery
- No full UI beyond minimal setup screen

Current pilot is running through `phone_module/script/`.
