# Android App Track (Archived)

This directory is archived and not part of the active runtime path.

Current project direction is **Termux-only phone forwarding** using:

- `phone_module/script/mpesa_forwarder.py`
- `phone_module/script/boot/start_forwarder.sh`

Use the script module docs for setup and operations:

- `phone_module/script/README.md`
