# Phone Module

This folder contains phone-side components used to feed the local-first ledger.

## Tracks

- `script/`: Termux-based SMS forwarder for pilot use (fastest path).
- `app/`: placeholder for future minimal APK forwarder.

## Current Recommendation

Start with `script/` to run a real pilot immediately while app work is prepared.
